// lib/ogPromptForge.ts

/**
 * Fonction fictive qui génère un prompt OG image à partir d’un titre.
 * À personnaliser plus tard selon ton usage réel.
 */
export function usePromptFromTitle(title: string): string {
  return `OG Image prompt pour : "${title}"`
}
